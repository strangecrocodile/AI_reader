# 复制本文件为 frontend/.env，即让前端连接真实后端。
# 不设置 VITE_API_BASE_URL 时，前端使用内置演示数据（纯前端模式，无需后端）。
#
# 也可以不改文件，只在启动时临时注入（Windows PowerShell）：
#   $env:VITE_API_BASE_URL='http://localhost:8000'; npm run dev

VITE_API_BASE_URL=http://localhost:8000
