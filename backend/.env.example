# 复制为 .env 并填写。未配置 LLM 时后端使用规则 mock 回退（演示仍可跑通）。

# ---- LLM（OpenAI 兼容接口；DeepSeek / SiliconFlow / 通义 DashScope 均可）----
LLM_BASE_URL=https://api.deepseek.com/v1
LLM_API_KEY=
LLM_MODEL=deepseek-chat
LLM_TIMEOUT=60

# ---- 向量嵌入（可选，开启后自动混合 BM25 + 向量检索）----
# EMBEDDING_URL=https://api.siliconflow.cn/v1
# EMBEDDING_API_KEY=
# EMBEDDING_MODEL=BAAI/bge-m3

# ---- 数据目录 ----
# AI_READER_DB=./data/ai_reader.db

# ---- 旧版 Word（.doc）----
# .doc 是 OLE 二进制格式，python-docx 读不了，需要本机装有 LibreOffice 才能解析
# （命令名 soffice / libreoffice 均可）。未装时 .doc 上传会给出明确提示，
# 其他格式不受影响。LibreOffice 不在 PATH 里时可用下面这项显式指定路径。
# AI_READER_SOFFICE=C:\Program Files\LibreOffice\program\soffice.exe
